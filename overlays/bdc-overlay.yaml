description: Overlay for kubernetes-core deployments to support SQL BDC.
machines:
  '0':
    constraints: cores=2 mem=4G root-disk=16G
  '1':
    constraints: cores=8 mem=64G root-disk=100G
applications:
  kubernetes-control-plane:
    options:
      allow-privileged: 'True'
 
